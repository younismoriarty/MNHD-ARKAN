services:
  madinat-misr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: madinat-misr-website
    ports:
      - "80:80"
    restart: unless-stopped
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: madinat-misr-backend
    volumes:
      - django-db-data:/app/db
    expose:
      - "8000"
    restart: unless-stopped

volumes:
  django-db-data:
